#include "solution.h"
#include <gtest/gtest.h>
#include <vector>

TEST(ThreeSumTest, TestCases) {
    Solution s;
    
    // TODO: Add test cases
    
    // Example test case format:
    // std::vector<int> nums = {-1, 0, 1, 2, -1, -4};
    // std::vector<std::vector<int>> expected = {{-1, -1, 2}, {-1, 0, 1}};
    // EXPECT_EQ(s.threeSum(nums), expected);
}